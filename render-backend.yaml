services:
  - type: web
    name: nrsa-backend
    env: node
    plan: free
    buildCommand: npm install && npm run build:server
    startCommand: node dist/server/index.js
    envVars:
      - key: NODE_ENV
        value: production